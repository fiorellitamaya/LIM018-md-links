const { pathExist } = require('../index');

describe('pathExist', () => {
  it('should be a function', () => {
    expect(typeof pathExist).toBe('function');
  });
  it('should be a function', () => {
    expect(typeof pathExist).toBe('function');
  });
});
